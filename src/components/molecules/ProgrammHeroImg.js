import HeroImg from '../atoms/HeroImg';
import { textData } from '../../data/textData';

export default function ProgrammHeroImg() {
    return (
        <HeroImg url={textData.programmHeroImg}/>
    )
}
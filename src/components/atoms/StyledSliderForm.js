import styled from 'styled-components';

const StyledSliderForm = styled.div `    
    .slider-container {
        grid-area: 2 / 2 / 3 / 3;
        height: 430px;
        width: 300px;
        background-color: #f5f5f5;
        overflow: hidden;
    }
`;

export default StyledSliderForm;